import 'package:flutter/material.dart';

class Background {
  static const Color mainbg = Color(0xFFE8E3CF);     // your main background
  static const Color containbg = Color(0xFFF4F1E4);  // cards / containers
  static const Color gridbg = Color(0xFFE2DCC7);     // grids / subtle sections
  static const Color calbg = Color(0xFFF4F1E4);      // calendar / highlight areas
  static const Color confirmbg = Color(0xFF8E9A5B);  // confirm buttons (accent)
  static const Color secbg = Color(0xFF8E9A5B);      // secondary accents
}

class TextCol {
  static const Color gentext = Color(0xFF5C563E);    // general text
  static const Color caltext = Color(0xFF5C563E);    // calendar / numbers
  static const Color txtcol = Color(0xFF8E9A5B);     // accent text (earnings, links)
  static const Color txtfield = Color(0xFFE8E3CF);   // text field backgrounds
  static const Color txtbtn = Color(0xFF8E9A5B);     // button text
}

class ButtonCol {
  static const Color btnbound = Color(0xFFB8AE8A);   // button borders
  static const Color mybtn = Color(0xFF8E9A5B);      // primary buttons
  static const Color btnIcon = Color(0xFF5C563E);    // icons inside buttons
  static const Color btnHi = Color(0xFF8E9A5B);      // highlight / selected buttons
  static const Color newbtn = Color(0xFFA4C8DF);
}